<h2><%= l(:label_estimated_time) %></h2>

<%= labelled_form_for @time_entry, :url => project_estimate_entry_path(@time_entry.project, @time_entry) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag l(:button_save) %>
<% end %>
